The complexity of this algorithm is O(log(n)) because it halves the search space in each iteration, similar to a binary search algorithm. This makes it an efficient solution for finding a peak in an unsorted list of integers.
